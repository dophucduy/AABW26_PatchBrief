# Frontend

React + Vite + TypeScript app for the Patch Brief file upload flow and report view.

```powershell
npm.cmd install
npm.cmd run dev
```

Type-check and production build:

```powershell
npx.cmd tsc --noEmit
npm.cmd run build
```
